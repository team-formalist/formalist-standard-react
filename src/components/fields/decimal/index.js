import float, { displayVariants as floatDisplayVariants } from '../float'
/**
 * Decimal fields are the same as floats as far as JavaScript is concerned
 */
export default float
export const displayVariants = floatDisplayVariants
