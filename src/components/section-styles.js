import { css } from 'emotion'

const base = css`
  margin-bottom: 5rem;
`

const label = css`
  border-bottom: 3px solid #36c;
  margin-bottom: 1.8rem;
  padding-bottom: 1.8rem;
`

export default {
  base,
  label,
}
